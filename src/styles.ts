import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  background: {
    backgroundColor: 'cream',
  },
  headerText: {
    fontSize: 30,
    color: 'black',
    textAlign: 'center',
  },
});
